<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyYoutubeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="statusBarBackground">#000</item>
        <item name="materialButtonStyle">@style/myOwnStyle</item>
    </style>

    <style name="myOwnStyle"></style>
    <style name="Theme.MyYoutubeApp" parent="Base.Theme.MyYoutubeApp" />
</resources>